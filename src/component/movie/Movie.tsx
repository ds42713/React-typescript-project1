import React, { useEffect, useState } from 'react'
import axios from 'axios'
import { movieInterface } from '../../interface/MovieInterface'

const baseUrl = "http://localhost:8001/movies"


export const Movie = () => {

   // const [movie, setMovie] = useEffect<movieInterface.Movie[]>([]) 

  return (
    <div>Movie</div>
  )
}
